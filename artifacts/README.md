# Local Evidence Boundary

This directory contains private call transcripts, user-specific task packets,
and historical debugging evidence. Those files stay local and are ignored by
Git.

Only this boundary note is published. Reproduce public-safe evidence with the
tests and compatibility commands in the repository; never publish raw call
records or user medical/contact data.
